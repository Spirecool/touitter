{% extends 'base.html.twig' %}

{% block title %}Touitts{% endblock %}

{% block body %}

    <div class="d-flex justify-content-around flex-wrap container mt-5">
        {% for post in posts %}
            <div class="card" style="width: 18rem;">
                {% if post.image %}
                    <img src="{{ post.image}} " alt="{{post.title}} class="card-img-top"">
                {% endif %}
                <div class="card-body">
                {% if post.title %}
                    <h5 class="card-title">{{ post.title }}</h5>
                {% endif %}
                    <p class="card-text">{{ post.content }}</p>
                    <a href="#" class="btn btn-danger">Supprimer</a>
                    <a href="#" class="btn btn-warning">Supprimer</a>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}



Lorem, ipsum dolor sit amet consectetur adipisicing elit. Fuga necessitatibus distinctio labore eum quos ad, quo non. Voluptatem, veritatis in!